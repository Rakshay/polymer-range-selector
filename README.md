# polymer-range-selector

### Installation

1. Install npm dependencies ```$ npm install```

2. Install bower dependecies ```$ npm run bower-install```

3. Start http-server ```npm run startServer```
